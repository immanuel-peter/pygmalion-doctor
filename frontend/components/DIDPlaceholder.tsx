export default function DIDPlaceholder() {
  return (
    <div className="w-full rounded-lg border border-dashed border-gray-400 bg-gray-50 p-6 text-sm text-gray-600">
      <h2 className="mb-2 text-base font-semibold text-gray-800">D-ID Agent</h2>
      <p className="mb-2">
        We&apos;re preparing an interactive video agent powered by D-ID. This section reserves the
        layout space so the experience remains stable while we finish the integration.
      </p>
      <p className="text-xs text-gray-500">
        Expect real-time conversation previews here once the avatar and audio pipeline are connected.
      </p>
    </div>
  );
}

